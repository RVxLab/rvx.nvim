local add, later = MiniDeps.add, MiniDeps.later

-- Needs support from the plugin
later(function()
    -- add({
    --     source = "ricardoramirezr/blade-nav.nvim",
    --     depends = {
    --         "saghen/blink.cmp",
    --     },
    -- })
    --
    -- require("blade-nav").setup({})
end)
