local palette = require("catppuccin.palettes").get_palette(require("catppuccin").flavour)

local M = {
    bg = palette.mantle,
    blue = palette.blue,
    green = palette.green,
    red = palette.red,
    orange = palette.peach,
    purple = palette.muave,
}

return M
